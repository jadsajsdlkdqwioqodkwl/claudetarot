{
  "name": "tarot-claude-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Product page COD en Cloudflare Workers, con Google Sheets y Evolution API",
  "scripts": {
    "dev": "wrangler dev",
    "check": "node scripts/check.mjs",
    "setup:sheet": "NODE_USE_ENV_PROXY=1 node scripts/setup-sheet.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
